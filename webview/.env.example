# Copy to `.env.local` and edit. `.env.local` is gitignored.
#
# WOWS_WORKSPACE — where the pipeline writes its artifacts.
# Must contain `libraries/accessories/index.json` and `ships/<Ship>/...`.
WOWS_WORKSPACE=I:\Models\warships
